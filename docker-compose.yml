version: '2.0'
services:
  indexer:
    image: iart/indexer
    build: .
    ports:
      - 50051:50051
    links:
      - redisai
      - elasticsearch
    volumes:
      - type: bind
        source: path/to/models
        target: /models
      - type: bind
        source: path/to/config.json
        target: /config.json
  redisai:
    image: redisai/redisai:1.0.1
  elasticsearch:
    image: elasticsearch:7.9.1
    environment:
      - xpack.security.enabled=false
      - discovery.type=single-node
    ulimits:
      memlock:
        soft: -1
        hard: -1
      nofile:
        soft: 65530
        hard: 65530
